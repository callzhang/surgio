{
  "name": "my-rule-store",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "update": "surgio generate && surgio upload",
    "start": "node server.js",
    "lint": "eslint . --ext .js",
    "format": "prettier --write ."
  },
  "engine": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@surgio/gateway": "2.0.1",
    "surgio": "^3.2.3"
  }
}
